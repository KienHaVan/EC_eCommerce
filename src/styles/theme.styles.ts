import { createTheme } from '@mui/material';

export const theme = createTheme({
  palette: {
    primary: {
      main: '#FFD333',
      light: '#FAF096',
    },
    secondary: {
      main: '#3D464D',
    },
    common: {
      red: '#D70000',
      green: 'rgba(0, 167, 17, 0.5)',
    },
  },
});
